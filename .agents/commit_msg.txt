fix remaining test regressions

Resolves the failures left after restoring a compilable test target:

- content/cleaner: update is_title_truncated test expectation to match current heuristic.
- cannibalization: write clusters and candidates artifacts to disk (not just DB) so downstream tests find the files; update missing-GSC test expectation for the new include-all-articles behavior.
- ctr_audit/template: restrict literal template-variable detection to site_template issues; update test for the new dual-result output.
- ctr_audit/tests: update meta-description range assertion to 120-155.
- keywords/tests: update workflow step count to 7 (ensure_coverage_fresh added).
- executor/tests: add clarity_project_id + content_hash/last_edited_at to in-memory schemas, set kimi_backend_mode=bridge in test DB, and switch seed-extraction/validation mocks to JSON-mode content responses.
- doctests: fix missing imports/conn in text.rs and task_store.rs examples.

cargo test now passes (634 lib tests + examples/doctests).
